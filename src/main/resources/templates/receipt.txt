{#if receipt.status is 'PENDING'}⌛️ PENDING {#else if receipt.status is 'IN_PROGRESS'}⚙️ IN PROGRESS {#else if receipt.status is 'ERROR'}‼️ ERROR {#else}❌ NO REG {/if}🧾

💳 Заказ #{receipt.orderNumber}
🕐 {time:format(receipt.createAt,"d MMM uuuu HH:mm:ss")}

{#if receipt.status == null || receipt.status is 'ERROR'}✅ /register_{receipt.orderNumber}{/if}
🚮 /cancel_register_{receipt.orderNumber}
{#if receipt.operId}🔁 /register_status_{receipt.operId}{/if}